# 发布说明

---

## 🚀 LitoBox v8.15.0 发布说明

### 🔄 从 v8.5.0 到 v8.15.0 更新内容

#### ✨ 新增工具

- **日志实时查看器** (v8.6.0) — 选择/拖入日志文件实时监控变化（类 tail -f），多关键词与/或过滤 + 8 色循环高亮，自动换行/滚动开关，虚拟滚动支持 5 万行流畅浏览（仅渲染可视区），四种主题（跟随系统/深色/浅色/护眼绿豆沙），UTF-8/GBK 自动识别，文件截断/轮转自动重载；Rust 后端 300ms 轮询零依赖
- **速查表扩展** (v8.7.0) — 12 个 Tab / 650+ 条记录：新增 Linux 常用命令（含 vim/ssh/tmux）、Git、SQL 通用语法、PostgreSQL/MySQL 差异对照表、Docker、Redis、Conda、PowerShell、NPM/Yarn/Pnpm 三系对照、正则表达式速查（元字符/断言/模板+陷阱提醒）；所有命令示例点击展开即可复制，数据独立文件组织可扩展
- **WebSocket 客户端** (v8.8.0) — ws/wss 连接、子协议、自动重连、消息收发与日志（时间/方向/大小着色）、请求头配置、心跳保活、消息历史导出
- **Markdown 幻灯片** (v8.8.0) — `---` 分割页面，左右键/空格翻页，全屏预览，导出单文件离线 HTML
- **思维导图（Markdown 版）** (v8.8.0) — Markdown 文本自动生成脑图（markmap），画布拖拽缩放、8 种主题配色、字体大小/字重分层，导出 PNG/SVG
- **思维导图编辑器（自由版）** (v8.9.0) — 基于 mind-elixir 的自由编辑脑图，4 种布局方向、8 套深浅主题、双击节点编辑、Tab/Enter 增子/兄节点、右键中文菜单（插入子/父/同级、删除、专注、摘要、关联线），数据存 SQLite 自动保存
- **TOTP 二次验证器** (v8.10.0) — otpauth 二维码/剪贴板/手动导入，多账号实时验证码 + 1 秒倒计时进度条，支持 SHA1/SHA256/SHA512 与 6/8 位
- **目录对比** (v8.10.0) — 递归对比左右文件夹的新增/删除/修改/相同，忽略规则支持 `*` 与 `?` 通配，可选 sha256 内容比对，扫描进度/取消 + 轮询兜底，差异明细按状态筛选 + 关键词搜索
- **代码实体生成** (v8.10.0) — JSON/JSON5 或 SQL DDL 输入，一键生成 TypeScript/Java/Go/Rust/Python/C# 六语言实体类，命名风格自动转换
- **本地大模型（Ollama）管理** (v8.11.0) — 服务状态检测、模型下载/删除/详情、运行监控与显存释放、流式对话测试，纯本地 API 调用无网络依赖
- **Ollama Python 工具调用** (v8.11.1) — 自定义 Python 工具（工具名/描述/参数 JSON Schema/Python 代码），模型自动决策调用，Rust 后端 stdin 传参执行 Python、stdout 返回结果，支持多轮工具结果自动回灌
- **文本排序增强** (v8.13.0) — 字符串工具批量处理 Tab 新增行排序操作组：升序/降序/忽略大小写/数字排序/自然排序/随机打乱/去重排序/按列排序，自定义分隔符与列索引
- **表情符号面板** (v8.13.0) — 26 分类 emoji 浏览（笑脸/动物/食物/技术/符号/旗帜/颜文字/数学/几何/货币等），搜索同时匹配字符与名称，hover 纯 CSS 零延迟显示名称，最近使用记录，点击复制
- **字体预览工具** (v8.13.0) — 100+ 常用字体列表，宽度对比法检测可用性，自定义预览文本/字号/粗细，搜索过滤，点击复制字体名
- **右键菜单管理** (v8.14.0) — 集中管理 Windows 文件/文件夹/桌面右键菜单项，读取静态项（`*\shell`、`Directory\shell`、`Directory\Background\shell`）与 COM 处理器（shellex\ContextMenuHandlers），COM 项自动解析 CLSID 显示名与 DLL 路径，系统内置项只读，第三方项可删除（删除前 reg export 精确备份），支持新增自定义菜单项，搜索 + 类型/归属筛选
- **Ping 连通性测试** (v8.15.0) — Rust 后端原生 ICMP ping，批量目标（域名/IP 逐个解析），自定义间隔/超时/次数，结果表格（丢包率/平均/最小/最大/抖动）实时刷新，历史记录
- **ASCII 艺术生成** (v8.15.0) — 文本转字符画（5×7 像素字库，8 种填充风格）；图片转 ASCII（灰度字符/彩色HTML/Braille盲文/半角方块 4 种输出），6 种字符集预设 + 自定义，彩色模式复制 HTML 粘贴富文本

#### 🔧 功能增强

- **速查表 Chmod 计算器** (v8.6.0) — Linux 文件权限 chmod 计算器：交互式勾选 rwx/SUID/SGID/sticky 位实时生成数字（777）与符号（rwxrwxrwx）表示，内置 10+ 常用权限预设
- **脑图画布增强** (v8.9.1) — 左键框选多节点、空白处右键菜单（居中/适配/全选）、视图状态持久化（每个脑图记住上次平移与缩放）、SVG 导出修复；SVG 工具预览区高度自适应 + 滚轮缩放，转 PNG 修复 canvas tainted
- **布局撑满与 Tab 保留** (v8.9.2) — 多处工具预览区改为弹性撑满剩余高度（文件处理、编码转换、文本对比、批量读取、Markdown、PDF、Hex、SVG 等）；各工具二级 Tab 切换改用 v-show 保留已输入内容（DevTools、磁盘空间分析、文件处理、编码转换等）
- **时间工具快捷复制** (v8.9.2) — 当前时间新增 16 种常用时间格式网格（中文年月日/横杠日期/紧凑日期/ISO 8601/UTC/时间戳等），随当前时间每秒刷新，逐行一键复制
- **Markdown 工具增强** (v8.9.2) — 输入区行数减半、新增 Ctrl+S 快捷保存
- **Ollama 多段会话管理** (v8.12.0) — 对话历史从单会话升级为会话列表，最多 20 段 × 200 条消息，历史弹窗支持新建/切换/删除/清空，标题自动取首条用户消息，旧版数据自动迁移；Python 工具执行修复（依次尝试 python/py/python3、子进程 UTF-8 输出、UTF-8/GBK 双编码解码）
- **工作流条件与循环** (v8.15.0) — 工作流步骤新增条件判断（总是/包含/不包含/等于/不等于/为空/不为空/正则匹配，不满足跳过）与循环执行（逐行处理：按行拆分逐行执行再合并；重复 N 次：以上次输出为输入重复执行），条件与循环可叠加，旧工作流自动兼容
- **StringTool 字符串工具优化** (v8.15.0) — 代码结构梳理、错误处理加固

### 📋 核心特性

- 🚀 零网络依赖 — 纯离线运行
- 📦 便携免安装 — 单 exe 文件
- 💾 数据本地化 — 不上传任何数据
- ⚡ 极速响应 — 启动 ≤ 1s
- 🧠 AI 驱动开发 — 全程零成本
- 🎨 深色/浅色主题 — 自动跟随系统

### 🛠️ 技术栈

- Vue 3 + TypeScript + Vite
- Tauri 2.0 (Rust)
- Element Plus
- SQLite + Pinia
- pdf-lib + pdfjs-dist（PDF 处理）
- crypto-js（加密解密）
- jsQR + qrcode + jsbarcode（二维码/条形码）
- mermaid + mind-elixir + markmap-lib（图表 / 脑图渲染）+ exifr（EXIF 元数据）
- SheetJS（Excel 处理）+ ajv / json-schema-faker（Schema 校验 / Mock）
- handlebars（模板渲染）+ @iarna/toml / js-yaml（配置解析）
- zip crate（压缩解压）+ onnxruntime-web / @paddleocr（OCR）
- CodeMirror 6（编辑器多主题/多语言高亮）
- opencc-js（繁简转换）+ pinyin-pro（拼音）+ mathjs（表达式计算）
- otpauth（TOTP）+ ollama（本地大模型 API）

## 如果提示安装 WebView2

- 下载 MicrosoftEdgeWebView2RuntimeInstallerX64.exe，使用管理员方式安装
---

## 🚀 LitoBox v8.5.0 发布说明

### 🔄 从 v8.0.1 到 v8.5.0 更新内容

#### ✨ 新增工具

- **Curl 构建器** (v8.2.0) — 表单实时生成 bash 风格 curl 命令（多行 `\` 续行），请求头/Basic·Bearer 认证/JSON·Form·Raw 三种请求体/Cookie/超时，认证与选项卡片默认折叠，本页生成历史最近 20 条一键还原；`{{变量}}` 占位符 + 变量表批量生成多条命令（逐条复制/全部复制），常用扩展参数（-L/-k/-i/-I/-s/-S/-v/-f/--compressed/-o/--connect-timeout/-x/-A），工作流可调用
- **变量命名转换** (v8.3.0) — 任意标识符/短语自动拆词，在 camelCase/PascalCase/snake_case/SCREAMING_SNAKE/kebab-case/dot.case/Title Case 7 种风格间互转，缩写词智能识别（HTTPServer→HTTP+Server、userID→user+ID），风格可勾选按需展示，多行批量，单元格点击复制、列头一键复制整列，工作流可调用
- **中文繁简转换** (v8.4.0) — OpenCC 词组级标准简繁互转（头发→頭髮、发生→發生 等多义字不误转），自动检测/简→繁/繁→简三种方向，混合文本整段转换，词典首次使用懒加载，工作流可调用
- **JSON 结构化对比** (v8.5.0) — 忽略对象键顺序、数组按索引的递归结构 diff，仅列新增(+)/删除(-)/修改(~) 节点并给出完整路径（a.b[0].c），完全相同子树零噪音；左右并排输入 500ms 防抖自动对比，路径与值点击即复制，交换左右/一键示例/复制差异文本

#### 🔧 功能增强

- **UUID 工具 → ID 生成器** (v8.0.2) — 雪花算法ID/MongoDB ObjectId/数字自增序列/NanoID/ULID/UUID v1·v5·v7/KSUID/CUID2/XID 共 12 种类型，随机数据同款卡片网格（独立配置/生成/批量复制 + 全部生成），每张卡片附使用场景说明，工作流可调用全部类型
- **随机数据扩充** (v8.0.2~v8.0.3) — 新增英文姓名/经纬度坐标/公司名称/个人档案/用户名/快递单号/金额（人民币大写）等类型，共 25 种按业务语义重排（个人信息→网络账号→企业财务→物流交通→地理标识→内容）；顶部统一数量一键全部生成、各卡片批量复制、大写开关实时重算；修复身份证第 17 位性别编码反了的问题
- **编码转换** (v8.1.0) — 新增人民币大写转换（数字→中文大写，支持万亿级、角分与零的正确处理，非法输入红色报错）；新增"多行批量"开关，URL/Base64/HTML实体/Unicode 编解码、时间戳双向转换、人民币大写均逐行转换，单行无效标记不中断整体
- **文本编辑器重构** (v8.2.1) — 修复行号与内容脱节、滚动条丢失；多文件 Tab（笔记库 + 本地文件同时多开，图标区分来源、脏标记 ●、关闭/切换自动保存确认）；打开本地文件自动识别 GBK/UTF-8/UTF-16 编码并对非 UTF-8 提示转码；新增 9 套编辑器主题（独立于应用主题并持久化）、VS Code 风格底部状态栏（行列/选中/语言/换行符/文件大小）、YAML/Shell/Bash 高亮与 22 种扩展名自动检测、自动换行热切换、Ctrl+G 跳转到行、Ctrl+S 保存与另存为分离
- **编辑器样式与 Tab 交互** (v8.2.2) — 修复打包后编辑器只显示行号、正文消失（生产构建向 style-src 注入 nonce 导致 CodeMirror 运行时注入样式被 CSP 拦截，同时修好 Element Plus 弹层等运行时样式）；编辑器多文件 Tab 新增右键菜单（关闭/关闭其他/关闭全部，未保存仍弹确认），修复关闭非激活 Tab 时本地文件修改被静默丢弃
- **首页与更新日志展示** (v8.1.0/v8.5.0) — 首页工具卡片展示功能描述并补齐各工具搜索关键词；更新日志页标题/详情智能拆分，长文自动换行并按需折叠展开

### 📋 核心特性

- 🚀 零网络依赖 — 纯离线运行
- 📦 便携免安装 — 单 exe 文件
- 💾 数据本地化 — 不上传任何数据
- ⚡ 极速响应 — 启动 ≤ 1s
- 🧠 AI 驱动开发 — 全程零成本
- 🎨 深色/浅色主题 — 自动跟随系统

### 🛠️ 技术栈

- Vue 3 + TypeScript + Vite
- Tauri 2.0 (Rust)
- Element Plus
- SQLite + Pinia
- pdf-lib + pdfjs-dist（PDF 处理）
- crypto-js（加密解密）
- jsQR + qrcode + jsbarcode（二维码/条形码）
- mermaid（图表渲染）+ exifr（EXIF 元数据）
- SheetJS（Excel 处理）+ ajv / json-schema-faker（Schema 校验 / Mock）
- handlebars（模板渲染）+ @iarna/toml / js-yaml（配置解析）
- zip crate（压缩解压）+ onnxruntime-web / @paddleocr（OCR）
- CodeMirror 6 + @uiw/codemirror-theme-*（编辑器多主题/多语言高亮）
- opencc-js（繁简词组级转换）+ pinyin-pro（拼音）+ mathjs（表达式计算）

## 如果提示安装 WebView2

- 下载 MicrosoftEdgeWebView2RuntimeInstallerX64.exe，使用管理员方式安装；相关文件在 v3.1 版本下面
---

## 🚀 LitoBox v8.0.1 发布说明

### 🔄 从 v7.2.0 到 v8.0.1 更新内容

#### ✨ 新增工具

- **IP 子网计算器** (v7.3.0) — IPv4 子网计算（网络地址/广播/掩码/反掩码/首末主机/主机数/IP类别/私网判断/二进制可视化，网络位与主机位分色高亮）、子网划分（按子网数量或每子网主机数）、IP 范围合并（起止 IP 转最少 CIDR 块）、IP↔整数互转，CIDR 与点分掩码两种输入
- **条形码生成工具** (v7.4.0) — CODE128/EAN-13/EAN-8/UPC-A/CODE39/ITF-14/Codabar/MSI/Pharmacode 9 种一维条码，条宽/条高/边距/前景色/背景色/显示文字可调，批量生成（CSV/TXT 导入，ZIP 打包下载，序号/文本命名）
- **拼音工具** (v7.5.0) — 中文转拼音，5 种输出模式（全拼带声调/无声调/首字母大写/驼峰式/全小写），分隔符可调，多音字候选展示，批量逐行处理，实时转换
- **剪贴板格式转换** (v7.7.0) — 一键读取剪贴板并自动识别类型，图片 ↔ Base64（DataURL）互转、HTML 富文本 → Markdown、表格（HTML table / TSV）→ Markdown / CSV / JSON / TSV
- **Excel 处理工具** (v7.8.0) — 多 Sheet 浏览切换、多文件合并（纵向追加行/横向拼接列）、数据清洗（去空行/去重/修剪空格/空值填充）、导出 CSV/JSON/Markdown/SQL INSERT 或另存 .xlsx
- **JSON Schema 工具** (v7.9.0) — draft-07 数据校验（错误路径/关键字/消息逐条展示）、一键生成 Mock 测试数据、导出 TypeScript 接口定义
- **模板渲染工具** (v7.10.0) — Handlebars 实时渲染，双栏编辑，渲染预览/HTML 源码双视图，快捷插入常用语法片段
- **Git 仓库统计** (v7.11.0) — 提交数/新增删除行数/贡献者排行、每日提交趋势图（30/90/365天）、文件改动 Top 10，异步后台分析不卡界面
- **图片对比** (v7.12.0) — 三种模式：滑动对比/差异高亮（容差可调，实时差异率）/半透明叠加，自动统一尺寸对齐，大图等比降采样保证性能
- **代码格式化 + 批量替换** (v7.13.0) — Prettier 多语言格式化（JS/TS/JSON/JSON5/CSS/SCSS/HTML/Vue/Markdown/YAML）+ 多文件内容搜索替换（正则模式，UTF-8/GBK 编码保留，替换前自动备份）
- **本地静态服务器** (v7.14.0) — 一键启动 HTTP 静态文件服务，自动展示本机+局域网地址，内建目录浏览页、子目录 ZIP 打包、网页端上传文件、实时访问日志，Rust 手写 HTTP/1.1 零依赖
- **压缩/解压工具** (v7.16.0) — ZIP 打包（压缩级别可调）、内容预览+勾选部分解压、密码加密（ZipCrypto 兼容 WinRAR/7-Zip / AES-256 解密）、防路径穿越、实时进度
- **Mermaid 图表渲染** (v7.17.0) — Mermaid 实时预览，内置 10 种模板，12 种主题与字号/背景可调，导出 SVG/PNG（2x 高清），工作流可调用
- **番茄钟** (v7.18.0) — 专注+休息循环（时长/间隔可自定义），Rust 后端秒级计时不卡界面，到点蜂鸣+系统通知双重提醒，全局快捷键 Ctrl+Alt+Shift+P
- **OpenAPI 解析** (v7.19.0) — OpenAPI 3.x/Swagger 2.0 解析，接口清单分组过滤，参数/请求体/响应 Schema 展示，一键生成 curl/fetch 示例与 Mock/TS 类型，工作流可调用
- **图片 EXIF 工具** (v8.0.0) — 即时读取 EXIF 信息（相机/镜头/拍摄参数/隐私字段），GPS 定位高亮（度分秒+十进制一键复制），一键清除隐私元数据（JPEG 无损剥离不损失画质），剥离前后大小对比

#### 🔧 功能增强

- **配置互转** (v7.6.0) — XML/YAML 工具新增"配置互转"Tab：JSON/YAML/TOML/INI/Java Properties 5 种格式环形互转，一键交换源↔目标格式，格式选择自动显示示例
- **媒体信息工具优化** (v7.14.1) — 支持图片文件（JPG/PNG/WebP/GIF/BMP/TIFF/AVIF/ICO）选择与预览，视频流卡片适配图像流展示，增补图片编解码器/像素格式/格式中文翻译
- **搜索键盘导航** (v7.14.2) — 首页搜索、全文搜索、命令面板、快速启动全程支持 ↑↓/Enter/Esc 键盘操作，无需鼠标
- **窗口体验优化** (v8.0.1) — 默认窗口尺寸从 1300×850 调小为 1100×720 适配 13/15 寸小屏笔记本；修复窗口尺寸记忆失效（补全 Tauri 2 窗口写操作权限 `core:window:allow-set-size/set-position/maximize`，此前恢复被权限拦截静默失败）；窗口尺寸/位置/最大化状态改由 Rust 后端持久化（window_state.json），启动时 setup 阶段窗口首次显示前恢复、退出确认兜底保存；新增启动提速：90 个工具页组件按需加载（首屏主包 4.8MB→103KB）、内联启动 loading 动画、窗口隐藏至界面就绪再显示（彻底消除启动白屏与尺寸跳变）

#### ❌ 已放弃

- **文件占用检测** (v7.15.0) — 曾实现句柄枚举方案，因 Windows API 环境兼容性问题（Restart Manager 挂起/句柄枚举受限）无法可靠检测，以后不再实现

### 📋 核心特性

- 🚀 零网络依赖 — 纯离线运行
- 📦 便携免安装 — 单 exe 文件
- 💾 数据本地化 — 不上传任何数据
- ⚡ 极速响应 — 启动 ≤ 1s
- 🧠 AI 驱动开发 — 全程零成本
- 🎨 深色/浅色主题 — 自动跟随系统

### 🛠️ 技术栈

- Vue 3 + TypeScript + Vite
- Tauri 2.0 (Rust)
- Element Plus
- SQLite + Pinia
- pdf-lib + pdfjs-dist（PDF 处理）
- crypto-js（加密解密）
- jsQR + qrcode + jsbarcode（二维码/条形码）
- mermaid（图表渲染）+ exifr（EXIF 元数据）
- SheetJS（Excel 处理）+ ajv / json-schema-faker（Schema 校验 / Mock）
- handlebars（模板渲染）+ @iarna/toml / js-yaml（配置解析）
- zip crate（压缩解压）+ onnxruntime-web / @paddleocr（OCR）

## 如果提示安装 WebView2

- 下载 MicrosoftEdgeWebView2RuntimeInstallerX64.exe，使用管理员方式安装；相关文件在 v3.1 版本下面
---

## 🚀 LitoBox v7.2.0 发布说明

### 🔄 从 v6.6.0 到 v7.2.0 更新内容

#### ✨ 新增工具

- **更新日志页面** (v7.2.0) — 读取 git 提交记录自动生成版本更新列表，按版本号时间线展示，支持按类型筛选（新增/修复/优化等）、关键词搜索、统计概览，侧边栏底部添加入口按钮（紫蓝渐变高亮 + 光泽动画）
- **密码保管箱** (v6.7) — 主密码保护（SHA-256 加密）、凭据增删改查/搜索筛选、密码一键复制、批量导入（Edge/Chrome CSV 自动去重）、批量删除、修改主密码（重加密所有凭据）、重置找回
- **文件批量重命名** (v6.8) — 替换/正则/前后缀/序号四种模式，实时预览确认，冲突检测，一键执行，撤销恢复
- **快速启动** (v6.9) — SQLite FTS5 全盘文件名索引，即时搜索，一键打开文件/目录（Alt+Space 呼出）
- **SVG 工具** (v6.12) — SVG 实时预览（透明棋盘格背景切换）、优化压缩（移除注释/metadata/空白）、转 PNG 栅格化（自定义尺寸/背景/保持比例/下载剪贴板）
- **二维码批量生成** (v6.13) — 每行一条文本/CSV 导入，批量生成多尺寸二维码，网格预览，ZIP 打包下载或单个下载/复制剪贴板，支持序号/文本内容命名

#### 🔧 功能增强

- **二维码美化** (v6.14) — 容错率可选（L/M/Q/H）、码点样式（方块/圆点/圆角）、渐变着色（亮度约束保证识别）、内嵌 Logo（大小按容错率自动约束）、自动验证（jsQR 解码校验）、智能风险提示（仅验证失败时高亮）、剪贴板读取 + Ctrl+V 粘贴解码
- **侧边栏折叠菜单** (v7.0) — 可折叠/展开，折叠状态仅图标+原生 tooltip，状态持久化，0.25s 过渡动画；侧边栏底部组件 tooltip 统一朝右；主题切换暗色模式修复
- **JSON 工具增强** (v7.0) — 数据统计（数组长度/对象属性数/嵌套总数）、Key 筛选（深度搜索多匹配）、布局优化（输入框缩小/操作+缩进合并/统计移到输出下方/筛选整合到输出标题栏）
- **编码工具 → 编码转换** (v7.1) — 新增 HTML实体/Unicode 编解码、时间戳转换、输出转输入、彩色分组卡片样式（URL/Base64/HTML实体/Unicode/AES/DES 等各色竖条区分）
- **哈希计算 → 加密解密** (v7.1) — 整合 AES/DES/3DES/RC4/Rabbit 对称加密，哈希算法扩展至 8 种（MD5/SHA-1/SHA-224/SHA-256/SHA-384/SHA-512/SHA-3/RIPEMD-160），算法说明 tooltip，Tab 切换哈希/加密
- **密码保管箱自动锁定** (v7.1.1) — 无操作自动锁定（默认关闭，可在设置中开启；锁定时长可选 1/3/5/10/15/30 分钟，默认 5 分钟），头部实时显示剩余倒计时徽章，全局活动检测，配置持久化到 SQLite
- **PDF 提取图片** (v7.1.2) — 从 PDF 中提取原始内嵌 XObject 位图资源，自动去重跨页复用图片，支持 JPEG/DCTDecode 原生字节保存、FlateDecode 压缩位图用浏览器 DecompressionStream 解压后经 Canvas 重编码为 PNG（支持 RGB/Gray/CMYK 颜色空间转换），卡片网格预览+格式彩色标签，单张保存+批量 ZIP 打包（含 _manifest.csv 清单）
- **PDF 加水印** (v7.1.4) — 支持文字/图片水印，9 宫格定位或平铺模式，可调节透明度、旋转角度、偏移量、字号/字体/颜色（文字）或图片尺寸（图片），纯前端 pdf-lib 实现，零额外依赖
- **视频加水印** (v6.10) — 文字/图片/两者叠加水印，9 宫格位置 + 自定义偏移，可选限定时间段，仅 ffmpeg 模式可用
- **剪贴板历史增强** (v6.11) — 自动捕获文本/图片/文件路径三种类型，统一列表 + 类型筛选标签，图片缩略图预览 + 大图查看，文件路径一键复制，图片缓存本地临时文件 + DB 索引


### 📋 核心特性

- 🚀 零网络依赖 — 纯离线运行
- 📦 便携免安装 — 单 exe 文件
- 💾 数据本地化 — 不上传任何数据
- ⚡ 极速响应 — 启动 ≤ 1s
- 🧠 AI 驱动开发 — 全程零成本
- 🎨 深色/浅色主题 — 自动跟随系统

### 🛠️ 技术栈

- Vue 3 + TypeScript + Vite
- Tauri 2.0 (Rust)
- Element Plus
- SQLite + Pinia
- pdf-lib + pdfjs-dist（PDF 处理）
- crypto-js（加密解密）
- jsQR + qrcode（二维码）

## 如果提示安装 WebView2

- 下载 MicrosoftEdgeWebView2RuntimeInstallerX64.exe，使用管理员方式安装；相关文件在 v3.1 版本下面
---

## 🚀 LitoBox v6.6.0 发布说明

### 🔄 从 v5.0 到 v6.6.0 更新内容

#### ✨ 新增工具

- **一键加速** (v6.6) — 内存释放（进程工作集回收）+ 临时文件清理 + 回收站清空，单进程内存详情查看，一键完整清理
- **证书查看器** (v6.6) — 浏览 Windows 证书存储，查看证书详情
- **环境变量编辑器** (v6.4) — 用户/系统双 Tab 切换，表格行内编辑，PATH 逐行列表编辑器，新增/删除弹窗，CSV 导出
- **开机启动项管理** (v6.3) — 注册表 Run 键 + 启动文件夹双数据源采集，启用/禁用/删除/新增操作，来源/状态筛选
- **计划任务管理器** (v6.2) — Windows 计划任务采集、启用/禁用/运行/删除，schtasks.exe 后端操作
- **网络连接查看器** (v6.1) — netstat 解析 + PowerShell fallback，连接列表/筛选/统计/自动刷新/结束进程/导出 CSV
- **快捷命令面板** (v6.0) — Ctrl+P 全局唤醒，搜索/键盘导航/分组显示，模糊匹配所有工具
- **Hosts 文件管理器** (v5.9) — hosts 文件编辑、Profile 管理（多套切换）、备份恢复、批量导入
- **全局快捷键占用查看器** (v5.8) — Windows 原生 API 探测全局热键占用，进程/窗口/快捷键对应关系展示
- **Windows 服务管理** (v5.6) — 服务列表查看、启动/停止/重启
- **媒体信息查看器** (v5.5) — ffprobe 视频/音频元数据查看，支持工作流集成
- **文字转语音** (v5.5) — 文本转语音播放与导出，历史记录与恢复

#### 🔧 功能增强

- **视频工具** (v5.5~v5.8) — 帧提取、区域裁剪、视频变速、旋转翻转、音量调整
- **CSV 导出规范统一** (v6.4) — 所有导出改用 `save_text_with_dialog` 保存对话框，BOM 前缀确保 Excel 识别，显示完整保存路径
- **环境变量编辑器交互** (v6.4) — 鼠标悬停 ✎ 图标提示、hover 高亮、底部操作指引
- **Hosts Profile 切换** (v5.9) — 切换前检查未保存修改、`onActivated` 去重
- **时间戳改为本地时间** (v5.9) — 备份/Profile 校验加固，增加 debug 日志
- **命令面板热键** (v6.0) — `db_read_shortcuts` 向后兼容合并默认热键，老用户自动补全 `__palette__`

#### 🐛 修复

- 一键加速按钮无需先扫描即可使用，修复临时文件和回收站不刷新问题
- 内存显示错误（28.7TB）— 改用 `GetProcessMemoryInfo` 获取准确工作集
- 一键加速扫描/执行改为 `async + spawn_blocking` 避免阻塞主线程
- 环境变量编辑器 — 改用 `Set-ItemProperty` 直接写注册表，消除广播延迟
- 环境变量编辑器 — `Add-Type` 定义顺序导致 `[User32]` 类型找不到
- 环境变量编辑器 — PATH 变量禁止外层删除，仅可在逐行编辑器中调整
- 环境变量编辑器 — 用 `[Environment]::SetEnvironmentVariable` 替代 P/Invoke，避免 Tauri 沙箱限制
- 命令面板热键空值覆盖 + Windows API 窗口恢复
- Hosts profile 名称白名单校验防止路径遍历
- Hosts `previewBackup` 保留换行 + confirm 错误处理统一 close
- Hosts 后端权限校验 + Tab 左内边距
- 快捷键占用查看器 — 探测状态锁死、TOCTOU 竞态、进程枚举未过滤、HWND 泄漏
- 快捷键占用查看器 — `handleComplete` 去重守卫 + 移除 BOM 字符
- 网络连接查看器 — 延迟初始加载 + 错误捕获，防止组件初始化崩溃
- 计划任务列表数据空白和操作列布局问题

## 🚀 LitoBox v5.0 发布说明

### 🔄 从 v4.3 到 v5.0 更新内容

#### ✨ 新增工具

- **音频裁剪工具** (v5.0) — 支持 MP3/WAV 单段裁剪，Canvas 波形可视化 + 滑块拖拽，Web Audio API 实时预览，ffmpeg 运行时探测加速，自定义导出路径，裁剪进度条展示
- **模板拼图** (v4.9) — CSS Grid 预设布局模板 + 点击交换，10 种布局模板、图片缩放/平移、透明背景
- **图标生成器** (v4.8) — 一张图生成多尺寸 PNG 图标和 .ico 文件，支持 Favicon / App Icon 预设
- **全文搜索工具** (v4.6) — 文件名/内容双模式搜索，正则表达式，进度取消，编码自动识别（UTF-8/GBK/UTF-16）
- **磁盘空间分析器** (v4.3) — 文件夹大小下钻、大文件 Top N、按扩展名统计、重复文件检测（前64KB指纹），支持勾选文件入回收站删除

#### 🔧 功能增强

- **系统工具后台采集** (v4.7) — 系统信息/网络/进程/硬件/软件 5 个采集页改为后台线程采集，采集期间不卡 UI、可切换其他工具，完成后应用内通知提示，点击跳回原页
- **进程 kill 与端口释放** (v4.4) — ProcessListView 加结束进程按钮，NetworkInfoView 监听端口加释放按钮，共享 kill_process 后端命令
- **多 Tab 导航模式** (v4.5) — 顶部 Tab 栏、同时打开多个工具、KeepAlive 状态保留、右键菜单关闭其他/全部、LRU 上限 8 个
- **侧边栏版本号动态读取** (v4.7) — 版本号从 package.json 动态读取，无需手动维护
- **图片增强工具** (v4.9) — 批量压缩转换、拼接、水印和调色板提取
- **图片拼图重构** (v4.9) — 从 Fabric.js 迁移到原生 CSS Grid 模板方案，移除 fabric 依赖，拖拽交换改为点击交换

#### 🐛 修复

- 模板拼图首次选择图片后槽位未填充的问题
- 模板匹配逻辑、预览清晰度、槽位内平移交互
- 全文搜索防竞态 —— 搜索启动后主动查状态兜底，解决快速完成的搜索事件被过滤卡住
- 取消检查改为 200ms 时间驱动，解决大目录下取消不响应
- 二进制检测加扩展名优先，解决 PDF 卡顿
- 去掉路径中的 `\\?\` UNC 前缀，展示更友好
- 音频预览按钮即时响应 + 停止取消加载
- 波形预览禁止右键菜单，修复预览重音问题
- 移除预览播放 60 秒上限限制

### 📋 核心特性

- 🚀 零网络依赖 — 纯离线运行
- 📦 便携免安装 — 单 exe 文件
- 💾 数据本地化 — 不上传任何数据
- ⚡ 极速响应 — 启动 ≤ 1s
- 🧠 AI 驱动开发 — 全程零成本
- 🎨 深色/浅色主题 — 自动跟随系统

### 🛠️ 技术栈

- Vue 3 + TypeScript + Vite
- Tauri 2.0 (Rust)
- Element Plus
- SQLite + Pinia
- Symphonia + Hound + LAME (音频处理)

## 如果提示安装 WebView2

- 下载 MicrosoftEdgeWebView2RuntimeInstallerX64.exe，使用管理员方式安装；相关文件在 v3.1 版本下面
---

## 🚀 LitoBox v4.3 发布说明

### 🔄 从 v3.1 到 v4.3 更新内容

#### ✨ 新增工具

- **磁盘空间分析器** (v4.3) — 文件夹大小下钻、大文件 Top N、按扩展名统计、重复文件检测（前64KB指纹），支持勾选文件入回收站删除
- **SQLite 查看器** (v4.1) — 只读查询、表结构浏览、CSV导出、查询历史记录
- **系统工具集** (v4.0) — 系统信息、网络信息、进程列表、硬件外设、软件环境
- **计算器工具** (v3.2) — 表达式计算、单位换算(11类)、日期计算、时间戳转换

#### 🔧 功能增强

- **历史记录增强** (v3.1) — 完整数据存储、双击跳转恢复、参数还原
- **文本编辑器** (v3.1) — CodeMirror 6 内核，语法高亮/查找替换/分类管理/自动保存
- **PDF↔OCR 联动** (v3.2) — PDF转图后一键识别，结果支持编辑校正
- **PDF 文本提取** (v4.2) — 保留段落换行，支持转 Markdown（文本提取+OCR双模式）
- **OCR/PDF/QR 结果编辑** (v4.2) — 识别结果支持编辑校正

#### 🎨 体验优化

- 页面缓存 KeepAlive，切换更流畅
- 编辑器明暗跟随系统主题
- 侧边栏固定工具置顶功能
- SQLite 数据持久化（v3.0）

### 📋 核心特性

- 🚀 零网络依赖 — 纯离线运行
- 📦 便携免安装 — 单 exe 文件
- 💾 数据本地化 — 不上传任何数据
- ⚡ 极速响应 — 启动 ≤ 1s
- 🧠 AI 驱动开发 — 全程零成本

### 🛠️ 技术栈

- Vue 3 + TypeScript + Vite
- Tauri 2.0 (Rust)
- Element Plus
- SQLite + Pinia


## 如果提示安装 WebView2

- 下载 MicrosoftEdgeWebView2RuntimeInstallerX64.exe，使用管理员方式安装；相关文件在 v3.1 版本下面
---
