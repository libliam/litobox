# V1.2 进阶版本实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现V1.2进阶版本的4个功能：窗口置顶、自定义快捷键、批量文本处理、文件编码转换

**Architecture:** 按复杂度递增顺序实现：窗口置顶(纯前端API) → 自定义快捷键(Rust插件) → 批量文本处理(纯前端) → 文件编码转换(Rust文件操作)

**Tech Stack:** Vue 3, TypeScript, Tauri 2.0, Rust, encoding_rs, tauri-plugin-global-shortcut

---

## 文件结构映射

### 新增文件
- `src/views/BatchTextTool.vue` - 批量文本处理页面
- `src/views/FileEncodingTool.vue` - 文件编码转换页面
- `src-tauri/src/file_encoding.rs` - Rust端文件编码转换逻辑

### 修改文件
- `src/store/index.ts` - 添加shortcuts配置和新工具定义
- `src/App.vue` - 注册新路由和热键监听
- `src/components/SidebarNav.vue` - 添加置顶按钮和版本号更新
- `src-tauri/Cargo.toml` - 添加新依赖
- `src-tauri/src/main.rs` - 注册Rust命令和热键

---

### Task 1: 窗口置顶快捷按钮

**Files:**
- Modify: `src/components/SidebarNav.vue`

- [ ] **Step 1: 添加置顶按钮到侧边栏**

在 `SidebarNav.vue` 的 `sidebar-footer` 区域添加置顶按钮：

```vue
<template>
  <aside class="sidebar">
    <!-- 现有内容保持不变 -->
    
    <div class="sidebar-footer">
      <button 
        class="pin-btn" 
        :class="{ active: isPinned }"
        @click="togglePin"
        :title="isPinned ? '取消置顶' : '窗口置顶'"
      >
        <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M12 2L12 12M12 12L8 8M12 12L16 8"/>
          <path d="M5 22L12 15L19 22"/>
        </svg>
      </button>
      
      <el-select v-model="currentTheme" size="small" class="theme-select">
        <el-option label="跟随系统" value="auto" />
        <el-option label="深色模式" value="dark" />
        <el-option label="浅色模式" value="light" />
      </el-select>
    </div>
  </aside>
</template>

<script setup lang="ts">
import { ref, computed, watch, onMounted } from 'vue'
import { useToolboxStore, TOOL_LIST } from '@/store'

// 现有代码保持不变...

// 置顶状态
const isPinned = ref(false)

// 切换置顶
const togglePin = async () => {
  try {
    const { getCurrentWindow } = await import('@tauri-apps/api/window')
    const currentWindow = getCurrentWindow()
    isPinned.value = !isPinned.value
    await currentWindow.setAlwaysOnTop(isPinned.value)
  } catch (error) {
    console.error('设置窗口置顶失败:', error)
  }
}

// 组件挂载时检查当前置顶状态
onMounted(async () => {
  try {
    const { getCurrentWindow } = await import('@tauri-apps/api/window')
    const currentWindow = getCurrentWindow()
    isPinned.value = await currentWindow.isAlwaysOnTop()
  } catch (error) {
    console.error('获取窗口置顶状态失败:', error)
  }
})
</script>

<style scoped>
/* 现有样式保持不变 */

.sidebar-footer {
  padding: 12px 16px;
  border-top: 1px solid var(--border-color);
  display: flex;
  align-items: center;
  gap: 8px;
}

.pin-btn {
  width: 28px;
  height: 28px;
  border: 1px solid var(--border-color);
  border-radius: 6px;
  background: var(--bg-input);
  color: var(--text-secondary);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.2s;
  flex-shrink: 0;
}

.pin-btn:hover {
  border-color: var(--accent-cyan);
  color: var(--accent-cyan);
}

.pin-btn.active {
  background: rgba(0, 212, 255, 0.15);
  border-color: var(--accent-cyan);
  color: var(--accent-cyan);
}

.theme-select {
  flex: 1;
}
</style>
```

- [ ] **Step 2: 验证功能**

运行开发服务器，点击置顶按钮验证窗口是否正确置顶/取消置顶。

- [ ] **Step 3: 提交**

```bash
git add src/components/SidebarNav.vue
git commit -m "feat: add window pin button for always-on-top toggle"
```

---

### Task 2: 自定义快捷键 - Rust端配置

**Files:**
- Modify: `src-tauri/Cargo.toml`
- Modify: `src-tauri/src/main.rs`

- [ ] **Step 1: 添加Rust依赖**

在 `src-tauri/Cargo.toml` 中添加：

```toml
[dependencies]
tauri = { version = "2", features = ["global-shortcut"] }
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
```

完整 `Cargo.toml`：

```toml
[package]
name = "litobox"
version = "1.2.0"
description = "栗的百宝箱"
authors = ["liam"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["global-shortcut"] }
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[features]
custom-protocol = ["tauri/custom-protocol"]
```

- [ ] **Step 2: 更新main.rs注册热键**

```rust
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

use tauri::{Manager, GlobalShortcutManager};
use std::collections::HashMap;

fn main() {
    tauri::Builder::default()
        .plugin(tauri_plugin_shell::init())
        .setup(|app| {
            // 注册默认快捷键
            let mut shortcuts = HashMap::new();
            shortcuts.insert("json", "CmdOrCtrl+Alt+J");
            shortcuts.insert("string", "CmdOrCtrl+Alt+S");
            shortcuts.insert("encode", "CmdOrCtrl+Alt+E");
            shortcuts.insert("regex", "CmdOrCtrl+Alt+R");
            shortcuts.insert("base", "CmdOrCtrl+Alt+B");
            shortcuts.insert("uuid", "CmdOrCtrl+Alt+U");
            
            let handle = app.handle().clone();
            
            for (tool_id, shortcut) in shortcuts {
                let tool = tool_id.clone();
                let h = handle.clone();
                
                app.global_shortcut_manager().register(shortcut, move || {
                    // 发送事件到前端
                    if let Some(window) = h.get_webview_window("main") {
                        let _ = window.emit("global-shortcut-triggered", &tool);
                    }
                }).unwrap_or_else(|e| {
                    eprintln!("注册快捷键 {} 失败: {}", shortcut, e);
                });
            }
            
            Ok(())
        })
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

- [ ] **Step 3: 提交**

```bash
git add src-tauri/Cargo.toml src-tauri/src/main.rs
git commit -m "feat: add global shortcut support with default keybindings"
```

---

### Task 3: 自定义快捷键 - 前端监听

**Files:**
- Modify: `src/App.vue`
- Modify: `src/store/index.ts`

- [ ] **Step 1: 更新store添加shortcuts配置**

在 `src/store/index.ts` 中：

```typescript
export interface ToolboxConfig {
  theme: 'auto' | 'dark' | 'light'
  jsonIndent: 2 | 4
  hotkey: string
  lastTool: string
  favorites: string[]
  shortcuts: Record<string, string>  // 新增
}

// 在默认配置中添加
const config = ref<ToolboxConfig>({
  theme: 'auto',
  jsonIndent: 2,
  hotkey: 'Ctrl+Alt+T',
  lastTool: 'home',
  favorites: ['json', 'string', 'encode'],
  shortcuts: {  // 新增
    json: 'CmdOrCtrl+Alt+J',
    string: 'CmdOrCtrl+Alt+S',
    encode: 'CmdOrCtrl+Alt+E',
    regex: 'CmdOrCtrl+Alt+R',
    base: 'CmdOrCtrl+Alt+B',
    uuid: 'CmdOrCtrl+Alt+U'
  }
})
```

- [ ] **Step 2: App.vue监听热键事件**

在 `src/App.vue` 的 `<script setup>` 中添加：

```typescript
import { onMounted, onUnmounted } from 'vue'

// 在现有 onMounted 中添加
onMounted(() => {
  // 现有主题代码...
  
  // 监听全局快捷键触发
  const unlisten = window.addEventListener('global-shortcut-triggered', (event: any) => {
    const toolId = event.detail
    if (toolId && toolId !== activeTool.value) {
      activeTool.value = toolId
      store.addRecentTool(toolId)
    }
  })
  
  // 保存unlisten函数用于清理
  ;(window as any).__shortcutCleanup = unlisten
})

onUnmounted(() => {
  if ((window as any).__shortcutCleanup) {
    (window as any).__shortcutCleanup()
  }
})
```

修正：Tauri事件监听应该使用 `@tauri-apps/api` 的 `listen`：

```typescript
import { onMounted, onUnmounted } from 'vue'
import { listen } from '@tauri-apps/api/event'

let unlistenShortcut: (() => void) | null = null

onMounted(async () => {
  applyTheme(store.config.theme)
  
  window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change', () => {
    if (store.config.theme === 'auto') {
      applyTheme('auto')
    }
  })
  
  // 监听全局快捷键
  unlistenShortcut = await listen('global-shortcut-triggered', (event) => {
    const toolId = event.payload as string
    if (toolId && toolId !== activeTool.value) {
      activeTool.value = toolId
      store.addRecentTool(toolId)
    }
  })
})

onUnmounted(() => {
  if (unlistenShortcut) {
    unlistenShortcut()
  }
})
```

- [ ] **Step 3: 提交**

```bash
git add src/store/index.ts src/App.vue
git commit -m "feat: add shortcut configuration and event listener in frontend"
```

---

### Task 4: 批量文本处理页面

**Files:**
- Create: `src/views/BatchTextTool.vue`
- Modify: `src/App.vue`
- Modify: `src/store/index.ts`

- [ ] **Step 1: 创建BatchTextTool.vue**

```vue
<template>
  <div class="tool-container">
    <div class="tool-card">
      <div class="card-header">
        <span class="card-title">批量操作</span>
      </div>
      <div class="card-body">
        <div class="action-grid">
          <div class="action-group">
            <span class="group-label">大小写</span>
            <el-button size="small" @click="applyBatch('toUpperCase')">转大写</el-button>
            <el-button size="small" @click="applyBatch('toLowerCase')">转小写</el-button>
            <el-button size="small" @click="applyBatch('capitalize')">首字母大写</el-button>
          </div>
          <div class="action-group">
            <span class="group-label">空格处理</span>
            <el-button size="small" @click="applyBatch('trim')">去除首尾空格</el-button>
            <el-button size="small" @click="applyBatch('removeAllSpaces')">去除所有空格</el-button>
            <el-button size="small" @click="applyBatch('normalizeSpaces')">规范化空格</el-button>
          </div>
          <div class="action-group">
            <span class="group-label">文本清理</span>
            <el-button size="small" @click="applyBatch('removeEmptyLines')">删除空行</el-button>
            <el-button size="small" @click="applyBatch('removeDuplicates')">去重</el-button>
            <el-button size="small" @click="applyBatch('reverseLines')">行反转</el-button>
            <el-button size="small" @click="applyBatch('sortLines')">行排序</el-button>
          </div>
        </div>
      </div>
    </div>

    <div class="tool-card">
      <div class="card-header">
        <span class="card-title">输入（每行一个文本片段）</span>
        <div class="card-actions">
          <el-button size="small" @click="handleClear">清空</el-button>
          <el-button size="small" @click="handlePaste">粘贴</el-button>
        </div>
      </div>
      <div class="card-body">
        <el-input
          v-model="inputText"
          type="textarea"
          :rows="10"
          placeholder="每行输入一个文本片段..."
          class="tool-textarea"
        />
      </div>
    </div>

    <div class="tool-card">
      <div class="card-header">
        <span class="card-title">输出结果</span>
        <div class="card-actions">
          <el-button size="small" @click="handleCopyAll">复制全部</el-button>
        </div>
      </div>
      <div class="card-body">
        <div v-if="results.length === 0" class="empty-state">
          暂无结果，请先输入文本并选择操作
        </div>
        <div v-else class="results-list">
          <div v-for="(result, index) in results" :key="index" class="result-item">
            <span class="result-index">{{ index + 1 }}</span>
            <div class="result-content">{{ result }}</div>
            <el-button size="small" @click="copyResult(index)">复制</el-button>
          </div>
        </div>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { ElMessage } from 'element-plus'
import { useToolboxStore } from '@/store'
import {
  toUpperCase,
  toLowerCase,
  capitalize,
  trim,
  removeAllSpaces,
  normalizeSpaces,
  removeEmptyLines,
  removeDuplicates,
  reverseLines,
  sortLines
} from '@/utils/stringUtils'

const store = useToolboxStore()
const inputText = ref('')
const results = ref<string[]>([])

const operationMap: Record<string, (text: string) => string> = {
  toUpperCase,
  toLowerCase,
  capitalize,
  trim,
  removeAllSpaces,
  normalizeSpaces,
  removeEmptyLines,
  removeDuplicates,
  reverseLines,
  sortLines
}

const applyBatch = (operation: string) => {
  if (!inputText.value.trim()) {
    ElMessage.warning('请先输入文本')
    return
  }

  const fn = operationMap[operation]
  if (!fn) {
    ElMessage.error('未知操作')
    return
  }

  const lines = inputText.value.split('\n')
  results.value = lines.map(line => fn(line))

  store.addHistory({
    tool: 'batch',
    action: operation,
    inputPreview: inputText.value.slice(0, 50),
    outputPreview: results.value.join('\n').slice(0, 50)
  })

  ElMessage.success(`已处理 ${results.value.length} 行文本`)
}

const handleClear = () => {
  inputText.value = ''
  results.value = []
}

const handlePaste = async () => {
  try {
    const text = await navigator.clipboard.readText()
    inputText.value = text
    ElMessage.success('粘贴成功')
  } catch {
    ElMessage.error('粘贴失败')
  }
}

const copyResult = async (index: number) => {
  try {
    await navigator.clipboard.writeText(results.value[index])
    ElMessage.success('已复制')
  } catch {
    ElMessage.error('复制失败')
  }
}

const handleCopyAll = async () => {
  try {
    await navigator.clipboard.writeText(results.value.join('\n'))
    ElMessage.success('已复制全部')
  } catch {
    ElMessage.error('复制失败')
  }
}
</script>

<style scoped>
.tool-container {
  height: 100vh;
  overflow-y: auto;
  padding: 20px;
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.tool-card {
  background: var(--bg-card);
  border: 1px solid var(--border-color);
  border-radius: 12px;
  overflow: hidden;
  transition: all 0.2s;
}

.tool-card:hover {
  border-color: rgba(0, 212, 255, 0.3);
}

.card-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 12px 16px;
  border-bottom: 1px solid var(--border-color);
  background: rgba(0, 212, 255, 0.03);
}

.card-title {
  font-size: 13px;
  font-weight: 600;
  color: var(--accent-cyan);
  text-transform: uppercase;
  letter-spacing: 1px;
}

.card-actions {
  display: flex;
  gap: 8px;
}

.card-body {
  padding: 16px;
}

.action-grid {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.action-group {
  display: flex;
  align-items: center;
  gap: 8px;
  flex-wrap: wrap;
}

.group-label {
  font-size: 12px;
  color: var(--text-muted);
  min-width: 70px;
}

.tool-textarea {
  width: 100%;
}

.empty-state {
  text-align: center;
  padding: 40px 20px;
  color: var(--text-muted);
  font-size: 13px;
}

.results-list {
  display: flex;
  flex-direction: column;
  gap: 8px;
  max-height: 400px;
  overflow-y: auto;
}

.result-item {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  padding: 10px 12px;
  background: var(--bg-input);
  border: 1px solid var(--border-color);
  border-radius: 8px;
  font-size: 13px;
}

.result-index {
  color: var(--accent-cyan);
  font-weight: 600;
  min-width: 24px;
  flex-shrink: 0;
}

.result-content {
  flex: 1;
  word-break: break-all;
  white-space: pre-wrap;
  color: var(--text-primary);
}

.results-list::-webkit-scrollbar {
  width: 4px;
}

.results-list::-webkit-scrollbar-track {
  background: transparent;
}

.results-list::-webkit-scrollbar-thumb {
  background: var(--border-color);
  border-radius: 2px;
}
</style>
```

- [ ] **Step 2: 更新App.vue注册路由**

在 `src/App.vue` 中：

```vue
<template>
  <!-- 在现有工具列表中添加 -->
  <BatchTextTool v-else-if="activeTool === 'batch'" />
  <FileEncodingTool v-else-if="activeTool === 'fileEncoding'" />
</template>

<script setup lang="ts">
import BatchTextTool from '@/views/BatchTextTool.vue'
import FileEncodingTool from '@/views/FileEncodingTool.vue'
// ... 其他导入
</script>
```

- [ ] **Step 3: 更新store添加新工具定义**

在 `src/store/index.ts` 的 `TOOL_LIST` 中添加：

```typescript
{ id: 'batch', name: '批量处理', icon: '📝', iconSvg: `<svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 00-2 2v16a2 2 0 002 2h12a2 2 0 002-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/><polyline points="10 9 9 9 8 9"/></svg>`, description: '批量文本处理', keywords: ['批量', '文本', '处理'] },
{ id: 'fileEncoding', name: '文件编码', icon: '📄', iconSvg: `<svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 00-2 2v16a2 2 0 002 2h12a2 2 0 002-2V8z"/><polyline points="14 2 14 8 20 8"/><path d="M9 15l2 2 4-4"/></svg>`, description: '文件编码转换', keywords: ['文件', '编码', '转换', 'utf', 'gbk'] }
```

- [ ] **Step 4: 提交**

```bash
git add src/views/BatchTextTool.vue src/App.vue src/store/index.ts
git commit -m "feat: add batch text processing tool"
```

---

### Task 5: 文件编码转换 - Rust端实现

**Files:**
- Create: `src-tauri/src/file_encoding.rs`
- Modify: `src-tauri/Cargo.toml`
- Modify: `src-tauri/src/main.rs`

- [ ] **Step 1: 添加Rust依赖**

在 `src-tauri/Cargo.toml` 中添加：

```toml
[dependencies]
encoding_rs = "0.8"
tauri-plugin-dialog = "2"
```

- [ ] **Step 2: 创建file_encoding.rs**

```rust
use encoding_rs::{Encoding, UTF_8, GBK, GB18030, ISO_8859_1};
use std::fs;

fn get_encoding(name: &str) -> &'static Encoding {
    match name.to_lowercase().as_str() {
        "utf-8" | "utf8" => UTF_8,
        "gbk" => GBK,
        "gb2312" | "gb18030" => GB18030,
        "iso-8859-1" | "latin1" | "iso8859-1" => ISO_8859_1,
        _ => UTF_8,
    }
}

#[tauri::command]
pub async fn read_file_with_encoding(path: String, encoding: String) -> Result<String, String> {
    let bytes = fs::read(&path).map_err(|e| format!("读取文件失败: {}", e))?;
    let enc = get_encoding(&encoding);
    let (decoded, _had_errors) = enc.decode(&bytes);
    Ok(decoded.into_owned())
}

#[tauri::command]
pub async fn convert_file_encoding(
    path: String,
    from_encoding: String,
    to_encoding: String,
    output_path: String,
) -> Result<String, String> {
    let bytes = fs::read(&path).map_err(|e| format!("读取文件失败: {}", e))?;
    
    let from_enc = get_encoding(&from_encoding);
    let (decoded, _) = from_enc.decode(&bytes);
    
    let to_enc = get_encoding(&to_encoding);
    let (encoded, _) = to_enc.encode(&decoded);
    
    fs::write(&output_path, encoded.as_ref())
        .map_err(|e| format!("写入文件失败: {}", e))?;
    
    Ok(output_path)
}

#[tauri::command]
pub async fn detect_file_encoding(path: String) -> Result<String, String> {
    let bytes = fs::read(&path).map_err(|e| format!("读取文件失败: {}", e))?;
    
    if bytes.starts_with(&[0xEF, 0xBB, 0xBF]) {
        return Ok("UTF-8-BOM".to_string());
    }
    
    let (decoded, _) = UTF_8.decode(&bytes);
    if decoded.chars().all(|c| !c.is_control() || c == '\n' || c == '\r' || c == '\t') {
        return Ok("UTF-8".to_string());
    }
    
    let (decoded_gbk, had_errors) = GBK.decode(&bytes);
    if !had_errors {
        return Ok("GBK".to_string());
    }
    
    Ok("Unknown".to_string())
}
```

- [ ] **Step 3: 更新main.rs注册命令**

在 `src-tauri/src/main.rs` 顶部添加：

```rust
mod file_encoding;
```

在 `.setup()` 之前添加 `.invoke_handler()`：

```rust
.invoke_handler(tauri::generate_handler![
    file_encoding::read_file_with_encoding,
    file_encoding::convert_file_encoding,
    file_encoding::detect_file_encoding
])
```

- [ ] **Step 4: 提交**

```bash
git add src-tauri/src/file_encoding.rs src-tauri/Cargo.toml src-tauri/src/main.rs
git commit -m "feat: add file encoding conversion commands in Rust"
```

---

### Task 6: 文件编码转换 - 前端页面

**Files:**
- Create: `src/views/FileEncodingTool.vue`

- [ ] **Step 1: 创建FileEncodingTool.vue**

```vue
<template>
  <div class="tool-container">
    <div class="tool-card">
      <div class="card-header">
        <span class="card-title">文件操作</span>
      </div>
      <div class="card-body">
        <div class="action-grid">
          <div class="action-group">
            <el-button size="small" @click="selectFile">选择文件</el-button>
            <el-button size="small" @click="detectEncoding" :disabled="!filePath">检测编码</el-button>
            <el-button size="small" type="primary" @click="convertFile" :disabled="!filePath || !targetEncoding">转换并保存</el-button>
          </div>
        </div>
        
        <div v-if="filePath" class="file-info">
          <span class="file-path">{{ filePath }}</span>
          <span v-if="detectedEncoding" class="encoding-badge">
            检测编码: {{ detectedEncoding }}
          </span>
        </div>
      </div>
    </div>

    <div class="tool-card">
      <div class="card-header">
        <span class="card-title">编码设置</span>
      </div>
      <div class="card-body">
        <div class="encoding-selectors">
          <div class="selector-group">
            <label>源编码:</label>
            <el-select v-model="sourceEncoding" placeholder="自动检测">
              <el-option label="自动检测" value="auto" />
              <el-option label="UTF-8" value="UTF-8" />
              <el-option label="GBK" value="GBK" />
              <el-option label="GB2312" value="GB2312" />
              <el-option label="ISO-8859-1" value="ISO-8859-1" />
            </el-select>
          </div>
          <div class="selector-group">
            <label>目标编码:</label>
            <el-select v-model="targetEncoding" placeholder="选择目标编码">
              <el-option label="UTF-8" value="UTF-8" />
              <el-option label="GBK" value="GBK" />
              <el-option label="GB2312" value="GB2312" />
              <el-option label="ISO-8859-1" value="ISO-8859-1" />
            </el-select>
          </div>
        </div>
      </div>
    </div>

    <div class="tool-card">
      <div class="card-header">
        <span class="card-title">内容预览</span>
        <div class="card-actions">
          <el-button size="small" @click="loadPreview" :disabled="!filePath">刷新预览</el-button>
        </div>
      </div>
      <div class="card-body">
        <div v-if="loading" class="loading-state">加载中...</div>
        <el-input
          v-else
          v-model="previewContent"
          type="textarea"
          :rows="15"
          placeholder="选择文件后预览内容..."
          class="tool-textarea"
          readonly
        />
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { ElMessage } from 'element-plus'
import { invoke } from '@tauri-apps/api/core'
import { open } from '@tauri-apps/plugin-dialog'
import { useToolboxStore } from '@/store'

const store = useToolboxStore()
const filePath = ref('')
const sourceEncoding = ref('auto')
const targetEncoding = ref('')
const detectedEncoding = ref('')
const previewContent = ref('')
const loading = ref(false)

const selectFile = async () => {
  const selected = await open({
    multiple: false,
    filters: [{ name: 'Text Files', extensions: ['txt', 'csv', 'log', 'md', 'json', 'xml', 'html'] }]
  })
  
  if (selected) {
    filePath.value = selected as string
    await loadPreview()
  }
}

const loadPreview = async () => {
  if (!filePath.value) return
  
  loading.value = true
  try {
    const enc = sourceEncoding.value === 'auto' ? 'UTF-8' : sourceEncoding.value
    previewContent.value = await invoke('read_file_with_encoding', {
      path: filePath.value,
      encoding: enc
    })
  } catch (error) {
    ElMessage.error(`读取文件失败: ${error}`)
  } finally {
    loading.value = false
  }
}

const detectEncoding = async () => {
  if (!filePath.value) return
  
  try {
    detectedEncoding.value = await invoke('detect_file_encoding', {
      path: filePath.value
    })
    ElMessage.success(`检测到编码: ${detectedEncoding.value}`)
  } catch (error) {
    ElMessage.error(`检测编码失败: ${error}`)
  }
}

const convertFile = async () => {
  if (!filePath.value || !targetEncoding.value) {
    ElMessage.warning('请选择文件和目标编码')
    return
  }
  
  const fromEnc = sourceEncoding.value === 'auto' 
    ? (detectedEncoding.value || 'UTF-8') 
    : sourceEncoding.value
  
  const outputPath = filePath.value.replace(/(\.[^.]+)$/, `_converted$1`)
  
  try {
    const result = await invoke('convert_file_encoding', {
      path: filePath.value,
      fromEncoding: fromEnc,
      toEncoding: targetEncoding.value,
      outputPath: outputPath
    })
    
    ElMessage.success(`转换成功: ${result}`)
    
    store.addHistory({
      tool: 'fileEncoding',
      action: `${fromEnc} -> ${targetEncoding.value}`,
      inputPreview: filePath.value,
      outputPreview: result
    })
  } catch (error) {
    ElMessage.error(`转换失败: ${error}`)
  }
}
</script>

<style scoped>
.tool-container {
  height: 100vh;
  overflow-y: auto;
  padding: 20px;
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.tool-card {
  background: var(--bg-card);
  border: 1px solid var(--border-color);
  border-radius: 12px;
  overflow: hidden;
  transition: all 0.2s;
}

.tool-card:hover {
  border-color: rgba(0, 212, 255, 0.3);
}

.card-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 12px 16px;
  border-bottom: 1px solid var(--border-color);
  background: rgba(0, 212, 255, 0.03);
}

.card-title {
  font-size: 13px;
  font-weight: 600;
  color: var(--accent-cyan);
  text-transform: uppercase;
  letter-spacing: 1px;
}

.card-actions {
  display: flex;
  gap: 8px;
}

.card-body {
  padding: 16px;
}

.action-grid {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}

.file-info {
  margin-top: 12px;
  padding: 10px 12px;
  background: var(--bg-input);
  border: 1px solid var(--border-color);
  border-radius: 8px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
}

.file-path {
  font-size: 13px;
  color: var(--text-primary);
  word-break: break-all;
  flex: 1;
}

.encoding-badge {
  font-size: 12px;
  color: var(--accent-cyan);
  background: rgba(0, 212, 255, 0.1);
  padding: 2px 8px;
  border-radius: 12px;
  border: 1px solid rgba(0, 212, 255, 0.3);
  white-space: nowrap;
}

.encoding-selectors {
  display: flex;
  gap: 24px;
  flex-wrap: wrap;
}

.selector-group {
  display: flex;
  align-items: center;
  gap: 8px;
}

.selector-group label {
  font-size: 13px;
  color: var(--text-secondary);
  white-space: nowrap;
}

.tool-textarea {
  width: 100%;
}

.loading-state {
  text-align: center;
  padding: 40px;
  color: var(--text-muted);
}
</style>
```

- [ ] **Step 2: 提交**

```bash
git add src/views/FileEncodingTool.vue
git commit -m "feat: add file encoding conversion tool UI"
```

---

### Task 7: 更新版本号和最终提交

**Files:**
- Modify: `src/components/SidebarNav.vue`
- Modify: `AGENTS.md`

- [ ] **Step 1: 更新版本号**

在 `SidebarNav.vue` 中：

```vue
<span class="app-version">v1.2</span>
```

- [ ] **Step 2: 更新AGENTS.md**

在 `AGENTS.md` 的 V1.2 部分添加完成标记：

```markdown
### V1.2 (进阶版本) ✅
- 批量文本处理
- 文件编码转换
- 窗口置顶快捷按钮
- 自定义快捷键
```

- [ ] **Step 3: 最终提交**

```bash
git add src/components/SidebarNav.vue AGENTS.md
git commit -m "release: update version to v1.2 with advanced features"
```

---

## 自审查

### 1. 规范覆盖检查
- ✅ 窗口置顶快捷按钮 - Task 1
- ✅ 自定义快捷键 - Task 2, 3
- ✅ 批量文本处理 - Task 4
- ✅ 文件编码转换 - Task 5, 6
- ✅ 版本号更新 - Task 7

### 2. 占位符扫描
- ✅ 无 TBD/TODO
- ✅ 所有函数都有具体实现
- ✅ 所有类型都有定义

### 3. 类型一致性
- ✅ store中的shortcuts配置与main.rs中的快捷键一致
- ✅ 工具ID在各文件中保持一致
- ✅ 编码名称在Rust和前端中统一

### 4. 范围检查
- ✅ 每个任务都是独立的，可以单独测试
- ✅ 按复杂度排序，逐步实现
